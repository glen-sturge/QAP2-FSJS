{
  "name": "qap2-fsjs",
  "version": "1.0.0",
  "description": "multi-route http server that displays html files. For each route an event will be emitted to log the server activity, this logging will be written to both disk and the console.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0"
  }
}
